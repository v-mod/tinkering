#include <stdio.h>

int main() {
    int day,month,year;
    int currentYear = 2024;
    int currentMonth = 09;
    int currentDay = 10;
    scanf("%d-%d-&d",&day,&month,&year);
    int yearsOld = currentYear - year;
    if (currentMonth >= month) {
        yearsOld = yearsOld - 1;
        int monthsOld = (12-month)
    } 
    return 0;
}